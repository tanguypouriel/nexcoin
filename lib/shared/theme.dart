import 'dart:ui';

const double largeRadius = 36;

const double mediumMargin = 16;

class ColorTheme {
  final Color cardBackground;

  const ColorTheme({required this.cardBackground});
}

const colorTheme = ColorTheme(cardBackground: Color(0xff222224));
